import styled from "styled-components";
import { typography, color, space, layout, border } from "styled-system";

const Text = styled.p`
    ${typography}
    ${color}
    ${space}
    ${layout}
    ${border}
`

export default Text